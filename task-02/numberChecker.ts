// Input bilangan
const number: number = 1000;

// Cek apakah genap
const isEven: boolean = number % 2 === 0;

// Menampilkan hasil
console.log(`${number} adalah bilangan ${isEven ? 'genap' : 'ganjil'}`); // Output: 1000 adalah bilangan genap